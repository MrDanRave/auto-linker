node_modules/

# Build artifact — uploaded to each GitHub release by the CI workflow
main.js

# Vault-specific plugin data (reject list, settings); never commit
data.json

# Semantic note-vector cache — derived from note content; never commit
embeddings.json

# Dev source maps
*.map
